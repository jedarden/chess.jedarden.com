{
  "name": "chess-jedarden-com",
  "version": "0.1.0",
  "description": "Local browser chess analysis",
  "scripts": {
    "prepare:engine": "node scripts/prepare-engine.mjs",
    "dev": "npm run prepare:engine && vite",
    "build": "npm run prepare:engine && vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "keywords": [],
  "author": "",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "dependencies": {
    "@lichess-org/stockfish-web": "^0.5.0",
    "agentation": "^3.0.2",
    "chess.js": "^1.4.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "vite": "^8.3.0"
  }
}
